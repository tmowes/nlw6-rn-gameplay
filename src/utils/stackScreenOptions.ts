import { StackNavigationOptions } from '@react-navigation/stack'

import { colors } from '../styles'

export const stackScreenOptions: StackNavigationOptions = {
  cardStyle: { backgroundColor: colors.secondary100 },
}
